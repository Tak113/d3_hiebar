dir
cd
tree

--activate local server for dev environment
open cmd
cd to the directly
type python -m http.server 8888 &. (python 2)
type py -m http.server 8080 &. (python 3 : currently only works)

go to google chrome, type http://localhost:8888/